
Git commands

$ git pull
$ git add *
$ git commit -m "Commit message"
$ git push origin master

$ git checkout -- <filename>
reverts changes in case of booboos


$ git log
shows recent commits
$ git log --pretty=oneline
more compact report

$ git log --graph --oneline --decorate$

